[project]
name = "netsage-backend"
version = "0.1.0"
description = "Backend for NetSage AI"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic",
    "pydantic-settings",
    "google-genai",
    "requests",
    "pytest",
    "pytest-cov",
    "ruff",
    "mypy",
]
